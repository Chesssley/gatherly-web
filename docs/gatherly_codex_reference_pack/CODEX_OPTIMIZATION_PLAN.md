
# Gatherly 网页优化修复计划（交给 Codex 使用）

## 推荐执行顺序

优先做小 bug，再做局部页面优化，最后做首页和登录注册的大重构。不要让 Codex 一次性修改所有问题。

| 阶段 | 建议 Issue | 类型 | 任务 | 参考图 | 风险 |
|---|---|---|---|---|---|
| A1 | BUG-01 | Bug | 私信系统点击关注后按钮从“关注用户”变为“已关注” | fig01 | 低/中 |
| A2 | BUG-02 | Bug | 邮箱验证码防泛滥提示显示剩余秒数后重试 | 无/现有弹窗 | 中 |
| B1 | ENH-01 | Enhancement | 个人主页粉丝/已关注分页、搜索去文字改图标 | fig01 | 中 |
| B2 | ENH-02 | Enhancement | 个人主页所有列表型板块统一分页 | fig01 | 中 |
| B3 | UI-02 | UI | 个人主页左侧信息栏删除用户邮箱显示 | fig01 | 低 |
| C1 | UI-03 | UI/Bug | 消息系统 UI 排版统一，查看详情后默认已读 | fig02 | 中 |
| D1 | UI-04 | UI | 未登录首页重构，按钮“加入聚场”，去掉左侧登录后模块 | fig04/fig08 | 高 |
| D2 | UI-05 | UI | 已登录首页按 fig14 重构，保留参考图功能按键对应区域 | fig03/fig14 | 高 |
| E1 | US-10-01 | User Story | 登录/注册/忘记密码弹窗化 | fig05/fig06/fig07/fig08 | 高 |
| E2 | US-10-02 | User Story | 注册后验证码与多步骤 onboarding | fig09-fig13 | 高 |

## Codex 总提示词

你是 Gatherly Web 项目的修复与 UI 重构助手。请先阅读：

- docs/reference/2026-06-04-optimization/README.md
- docs/reference/2026-06-04-optimization/reference-images/contact_sheet.jpg
- 当前仓库中的 README、app/templates、app/routes、app/static/css/style.css、app/static/js/main.js

统一约束：

- 保持 Flask + Jinja2 + 原生 HTML/CSS/JS，不引入 Vue、React、Bootstrap。
- 不要直接改 main，不要改无关文件。
- 只完成当前 Issue，不要把后续阶段一起做。
- 不要删除已有 class/id，避免破坏 JS。
- 图片仅作参考，不要直接复制为网站素材。
- 中文文案统一使用「聚场 / Gatherly」。
- 修改后说明：改了哪些文件、如何自测、是否涉及数据库 migration。

## A1：BUG-01 私信关注按钮状态

Codex 提示词：

```text
当前只修复 [BUG-01] 私信系统点击关注后弹窗按钮状态问题。
目标：用户在私信系统或用户详情弹窗中点击“关注用户”后，如果后端关注成功，弹窗里的按钮立即变为“已关注”，并禁用或切换成取消关注入口，不能仍显示“关注用户”。
参考图：docs/reference/2026-06-04-optimization/reference-images/fig01_profile_follow_status_pagination.png
请先搜索项目中 follow、关注用户、已关注、message、profile、user modal 相关代码。
允许修改：消息/个人主页相关模板、对应 route、少量 JS、少量 CSS。
禁止修改：数据库字段、登录注册、首页大布局。
验收：点击关注后无需刷新即可看到“已关注”；刷新页面后状态仍正确；失败时显示错误提示。
```

## A2：BUG-02 验证码频率限制倒计时

```text
当前只修复 [BUG-02] 邮箱验证码防泛滥提示问题。
目标：当用户触发验证码发送频率限制时，弹窗或提示信息必须显示“请在 X 秒后重试”，并能随着时间更新剩余秒数；倒计时结束后允许重新发送。
请先搜索 send_code、verification、验证码、rate limit、cooldown、email 相关代码。
允许修改：auth route、验证码相关模板/JS、少量 CSS。
禁止修改：首页、个人主页、活动模块。
验收：重复点击发送验证码时显示剩余秒数；秒数递减；倒计时结束按钮恢复可点击；后端仍然阻止绕过前端倒计时的频繁请求。
```

## B1-B3：个人主页分页、搜索、隐藏邮箱

```text
当前只完成个人主页优化，不要修改首页和登录注册。
任务：
1. 左侧信息栏底部粉丝与已关注用户板块增加分页。
2. 优化搜索界面：删除“搜索用户”文字按钮，将搜索按钮改为图标形式。
3. 个人主页所有列表展示型板块加入分页。
4. 删除个人主页左侧信息栏中的用户邮箱显示。
参考图：fig01_profile_follow_status_pagination.png。
请先搜索 profile、followers、following、粉丝、已关注、搜索用户 相关模板和路由。
验收：列表超过每页数量时出现上一页/下一页或页码；搜索仍可用；邮箱不再展示；移动端不溢出。
```

## C1：消息系统 UI 与已读状态

```text
当前只优化消息系统 UI 和已读状态。
目标：统一消息系统的 UI 排版、文字和按钮位置；用户点击“查看详情”之后，该条消息默认变为已读状态。
参考图：fig02_notification_center_read_status.png。
请搜索 notification、message、inbox、read、unread、查看详情、全部标为已读 相关代码。
验收：消息列表排版统一；未读/已读视觉区别清晰；点击详情后数据库/后端状态变为已读；返回列表后不再显示未读。
```

## D1：未登录首页重构

```text
当前只重构未登录首页，不要修改登录注册弹窗逻辑。
目标：未登录用户打开首页时，视觉参考 Meetup 首页；左侧信息栏的三个登录后模块不显示；主按钮文案为“加入聚场”；中间主文案使用中文，围绕文案生成/使用类似图4氛围的装饰插图布局。
参考图：fig04_meetup_guest_home_reference.png、fig08_meetup_finish_signup_full_reference.png。
请保持 Gatherly 品牌，不要使用 Meetup logo 和英文文案。
验收：未登录首页没有登录后模块；按钮为“加入聚场”；点击按钮打开注册弹窗或跳转注册入口；移动端布局正常。
```

## D2：已登录首页重构

```text
当前只重构已登录首页，不要修改注册流程。
目标：已登录首页完全按 fig14 的结构重构：顶部搜索/城市入口，左侧用户信息卡，For you 活动横向流，From your groups/circles 区域，保留参考图中对应功能按钮，但用 Gatherly 中文文案。
参考图：fig03_current_logged_home_problem.png 是当前问题，fig14_meetup_logged_home_reference.png 是目标。
验收：登录后首页与未登录首页分支清晰；左侧模块只在登录后出现；活动卡片功能不丢失；点击活动仍能进入详情；不破坏现有活动查询和标签数据。
```

## E1：登录/注册/忘记密码弹窗化

```text
当前只重构登录、注册、忘记密码为弹窗形式，不做 onboarding 步骤。
参考图：fig05、fig06、fig07、fig08。
目标：登录、注册、忘记密码在首页上以居中弹窗出现，背景遮罩；注册表单填写完成后点击注册自动发送验证码，并切换到验证码输入界面。
请保持后端安全校验，不要只做前端假流程。
验收：登录弹窗可打开/关闭；注册弹窗可切换；忘记密码可打开；表单错误提示清晰；验证码发送成功后进入验证码输入界面。
```

## E2：注册验证码与多步骤 onboarding

```text
当前只做注册验证码通过后的完成注册步骤，不要重构首页。
参考图：fig09、fig10、fig11、fig12、fig13。
目标：验证码正确后进入完成注册/新用户引导步骤：使用目的、生日/年龄、性别/偏好、兴趣选择、推荐圈子/完成。使用中文文案，并根据 Gatherly 主题调整字段。
如果需要新增字段，请先说明是否必须改 models.py；优先复用现有 User 字段和兴趣标签关系，避免不必要迁移。
验收：验证码错误不能进入下一步；正确后进入步骤页；步骤有进度条；兴趣选择至少 3 个；完成后进入已登录首页。
```
